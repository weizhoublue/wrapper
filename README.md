# wrapper

一次性 AI 编码代理 CLI 封装器。

```bash
npm install
npm test

# 运行 agent（提示词为最后一个参数）
wrapper run -t codex "say hi"

# throttle 管理
wrapper throttle -l
wrapper throttle -d 1

wrapper -h
wrapper run -h
```

详见 `docs/get-started.md`、`docs/throttle.md`。
